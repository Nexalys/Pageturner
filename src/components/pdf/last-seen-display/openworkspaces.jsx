export function OpenWorkspaces({ list }) {
    return (
        <div className='flex flex-col gap-3 text-sm text-text-sc'>
            {list && list.map(
                (name, ind) => {

                }
            )}

        </div>
    )
}